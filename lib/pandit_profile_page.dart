import 'package:flutter/material.dart';

class PanditProfilePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pandit Profiles'),
      ),
      body: Center(
        child: Text('List of Pandit Profiles will go here'),
      ),
    );
  }
}
